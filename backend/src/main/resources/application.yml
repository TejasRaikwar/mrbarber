server:
  port: 8081
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:mrbarber}?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=${DB_USE_SSL:false}&sslMode=${DB_SSL_MODE:PREFERRED}&enabledTLSProtocols=TLSv1.2,TLSv1.3
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:160702}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

app:
  jwt:
    # Base64-encoded secret. Generate one with: openssl rand -base64 64
    secret: ${JWT_SECRET:c3VwZXJzZWNyZXRtcmJhcmJlcmp3dHNpZ25pbmdrZXl0aGF0c2hvdWxkYmVjaGFuZ2VkaW5wcm9k}
    expiration-ms: 86400000  # 24h
  cloudinary:
    url: ${CLOUDINARY_URL:cloudinary://725234339843327:L-FSuwgmlaPG9p3CpxBGI1vCvw4@dfycl4q5v}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:4173}
  seed:
    admin-username: ${ADMIN_USERNAME:admin}
    admin-password: ${ADMIN_PASSWORD:admin123}

logging:
  level:
    org.springframework.security: INFO
